export function defineKeysOfRecord() {
    return function defineMatchingKeys(keys, truth) {
        if (!truth)
            throw new Error(`Argument truth must be true for type checking to work`);
        return keys;
    };
}
