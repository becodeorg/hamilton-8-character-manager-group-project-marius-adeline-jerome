import { UploadFileResponse } from "./upload-types";
/**
 * Import URL as Image Props
 *
 * Takes a `url` to an image and then imports it into S3 at the associated
 * `path`.
 */
export declare type ImportImageProps = {
    authToken: string;
    path: string;
    data: {
        url: string;
    };
};
export declare type ImportImageResponse = UploadFileResponse;
